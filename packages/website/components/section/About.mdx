import { EditMe } from '../../components/Github';

# 关于微狐科技

##  WIP

```
  return (
    <Container className={classes.root}>
      <HiddenPrefetch />
      <BottomNavigation value={value} onChange={handleChange} className={classes.root}>
        <BottomNavigationAction label="wip" value="/wip" icon={<RestoreIcon />} />
        <BottomNavigationAction label="wip" value="/wip" icon={<ListIcon />} />
        <BottomNavigationAction label="wip" value="/wip" icon={<EmojiPeopleIcon />} />
        {/* <BottomNavigationAction label="关于" value="/about" icon={<InfoIcon />} /> */}
      </BottomNavigation>
    </Container>
  );
```

```jsx
  return (
    <Container className={classes.root}>
      <HiddenPrefetch />
      <BottomNavigation value={value} onChange={handleChange} className={classes.root}>
        <BottomNavigationAction label="wip" value="/wip" icon={<RestoreIcon />} />
        <BottomNavigationAction label="wip" value="/wip" icon={<ListIcon />} />
        <BottomNavigationAction label="wip" value="/wip" icon={<EmojiPeopleIcon />} />
        {/* <BottomNavigationAction label="关于" value="/about" icon={<InfoIcon />} /> */}
      </BottomNavigation>
    </Container>
  );
```

```js
  return (
    <Container className={classes.root}>
      <HiddenPrefetch />
      <BottomNavigation value={value} onChange={handleChange} className={classes.root}>
        <BottomNavigationAction label="wip" value="/wip" icon={<RestoreIcon />} />
        <BottomNavigationAction label="wip" value="/wip" icon={<ListIcon />} />
        <BottomNavigationAction label="wip" value="/wip" icon={<EmojiPeopleIcon />} />
        {/* <BottomNavigationAction label="关于" value="/about" icon={<InfoIcon />} /> */}
      </BottomNavigation>
    </Container>
  );
```

<EditMe path="components/section/About.mdx" />
